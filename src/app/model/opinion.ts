export interface MyComment {
  title: string;
  author: string;
  comment: string;
  date: string;
  nStar: number;
}

export interface Opinion {
  percentage: string;
  nStart: number;
  coincidence: number;
}
